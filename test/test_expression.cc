#include "adlib/atomic.h"

int main()
{
  a = adlib.SymPrimitive();
  b = adlib.SymPrimitive();

  c = adlib.mul(a,b);
  d = adlib.add(m,a)

  d.eval_fcn(Assignment({a,b},{4,2}));

  F = Function({a,b},{d});
  F(4,2);

}
